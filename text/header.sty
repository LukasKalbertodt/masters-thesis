\ProvidesPackage{header}

% Basic layout
\usepackage[a4paper, left=3cm, right=3cm, top=2.9cm, bottom=2.9cm, footskip=1.3cm]{geometry}

% Everything related to language
\usepackage[english]{babel}

% Some other packages
\usepackage{blindtext}
\usepackage[pageanchor=false]{hyperref} % hyperlinks

% Setup fonts
%
% So I still have to decide. Either use CM + Biolinum as sans font, or
% Libertine (including Biolinum) everywhere. OR even Palatino?
\usepackage[T1]{fontenc}
% \usepackage{mathpazo}
\usepackage[mono=false]{libertine}
\usepackage[libertine]{newtxmath}
% the 0.93 was set to match the x height of libertine font
\setmonofont[Scale=0.93]{Inconsolata}
% \usepackage{biolinum}
% \usepackage{lmodern}

\usepackage{titlesec}
\titleformat{\chapter}[display]
    {\sffamily\raggedleft\fontsize{50}{0}\selectfont\bfseries}
    {\thechapter}
    {-.5em}
    {\huge\raggedright}
    [\hrule]
\titleformat{\section}[block]
    {\sffamily\Large\bfseries}{\thesection}{1em}{}


% Stuff for including images
\usepackage{graphicx}
\graphicspath{ {img/} }


% Colors
\usepackage{xcolor}
\definecolor{lightgray}{rgb}{0.83, 0.83, 0.83}
\definecolor{solarized_bg}{HTML}{FDF6E3}

% Multi-line source code
\usepackage[outputdir=build]{minted}
\usemintedstyle{mysolarized}   % see the `solarized` folder for more info
\setminted{
  linenos,
  numbersep=10pt,
  xleftmargin=20pt,
  xrightmargin=10pt,
  frame=single,
  framesep=7pt,
  baselinestretch=1.05,
  fontsize=\small     % maybe \footnotesize
}
\newminted{rust}{
    rulecolor=\color{lightgray},
}
\newminted{cpp}{
    rulecolor=\color{lightgray},
}

% Inline code boxes
\usepackage{newverbs}
\usepackage[skins]{tcolorbox}
\newtcbox{\codebox}[0]{
    on line,
    colback=solarized_bg!30,
    colframe=solarized_bg!95!black,
    size=fbox,
    arc=3pt,
    boxrule=0.8pt,
    boxsep=1pt,
    top=1pt,
    bottom=1pt,
}
\newcommand{\code}{\collectverb{\codebox}}
